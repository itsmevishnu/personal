#Example
```
<?php

namespace App;

use Illuminate\Database\Eloquent\Model;
use App\User;

class Message extends Model
{
    protected $table = 'messages';
    protected satic $name = 'User';

    public function testFunction()
        {
            if ($value == true) {
            return true;
        } else {
            $value = true;
            testFunction();
      }
    }
}

```
[Home](../index.md)
